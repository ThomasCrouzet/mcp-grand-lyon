"""Données empaquetées (templates de config + fixtures de démo offline).

Résolues via :mod:`grand_lyon_mcp.resources` avec ``importlib.resources`` afin de
fonctionner aussi bien depuis un checkout que depuis un wheel installé (``pip``/``uvx``).
"""
